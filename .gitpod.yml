image:
  file: .gitpod.Dockerfile

tasks:
  - name: Auto-detect demo from folder
    init: |
      echo "🚀 Starting Docker containers..."
      docker-compose up -d

      echo "⏳ Waiting for PostgreSQL to be ready..."
      for i in {1..15}; do
        if psql -h localhost -p 5432 -U pguser -d pgdb -c '\q' 2>/dev/null; then
          echo "✅ PostgreSQL is ready!"
          break
        else
          echo "Attempt $i: PostgreSQL not ready yet"
          sleep 3
        fi
      done

      echo "⏳ Waiting for RisingWave to be ready..."
      for i in {1..15}; do
        if psql -h localhost -p 4566 -U root -d dev -c '\q' 2>/dev/null; then
          echo "✅ RisingWave is ready!"
          break
        else
          echo "Attempt $i: RisingWave not ready yet"
          sleep 3
        fi
      done

      echo "🐍 Installing Python + psycopg2..."
      sudo apt-get update
      sudo apt-get install -y python3-pip
      pip3 install psycopg2-binary

      # Detect demo folder from path
      DEMO_PATH=$(pwd | grep -o '02-simple-demos/.*')
      echo "📁 Auto-detected DEMO_PATH: ${DEMO_PATH}"

      if [ -d "${DEMO_PATH}" ]; then
        if [ -f "${DEMO_PATH}/pg_bootstrap.sql" ]; then
          echo "📦 Loading Postgres bootstrap..."
          PGPASSWORD=pgpass psql -h localhost -p 5432 -U pguser -d pgdb < ${DEMO_PATH}/pg_bootstrap.sql
        fi
        if [ -f "${DEMO_PATH}/bootstrap.sql" ]; then
          echo "📦 Loading RisingWave bootstrap..."
          psql -h localhost -p 4566 -U root -d dev < ${DEMO_PATH}/bootstrap.sql
        fi
        if [ -f "${DEMO_PATH}/data_generator.py" ]; then
          echo "⚙️ Running data generator..."
          python3 ${DEMO_PATH}/data_generator.py
        fi
      else
        echo "⚠️ Could not detect a valid demo folder."
      fi

ports:
  - port: 8088
    onOpen: open-preview
  - port: 4566
    onOpen: ignore
  - port: 5432
    onOpen: ignore
  - port: 9092
    onOpen: ignore
  - port: 2181
    onOpen: ignore

vscode:
  extensions:
    - ms-azuretools.vscode-docker
